# 飞书群聊消息拉取 (feishu-group-summary)

搜索飞书群聊,拉取指定时间范围内的历史消息,输出为易读的 Markdown 格式。

## 功能特性

- 🔍 **智能搜索**: 通过关键词搜索飞书群聊
- 📥 **批量拉取**: 自动处理分页,拉取完整历史消息
- 📝 **Markdown 输出**: 按时间顺序组织,易读易分享
- 🕐 **灵活时间范围**: 支持自然语言描述("近3天"、"上周")和精确时间戳
- 🤖 **LLM 友好**: 输出格式直接供 Claude 等 LLM 分析

## 安装

### 1. 确保已安装 lark-cli

```bash
# 检查 lark-cli 是否已安装
lark-cli --version

# 如果未安装,请先安装
# (安装方式请参考 lark-cli 文档)
```

### 2. 安装插件

在 Claude Code 中:

```bash
/marketplace add /Users/bytedance/Demo/my-plugins/.claude-plugin/marketplace.json
```

或者直接将 `marketplace.json` 拖入 Claude Code 界面。

## 使用方法

### 基本用法

在 Claude Code 中直接说出你的需求:

```
帮我拉取项目群最近3天的聊天记录
```

```
查看产品团队群上周的消息
```

```
导出设计群本周的聊天记录
```

### 核心脚本

插件提供一个简洁的核心脚本 `fetch_chat_messages.py`:

#### 搜索群聊

```bash
python3 scripts/fetch_chat_messages.py search "关键词"
```

#### 拉取消息

```bash
# 使用时间范围描述
python3 scripts/fetch_chat_messages.py fetch \
  --chat-id oc_xxx \
  --chat-name "群名称" \
  --time-range "近3天" \
  --output messages.md

# 使用时间戳
python3 scripts/fetch_chat_messages.py fetch \
  --chat-id oc_xxx \
  --start-time 1642723200 \
  --end-time 1642992000 \
  --output messages.md

# 输出到终端(不保存文件)
python3 scripts/fetch_chat_messages.py fetch \
  --chat-id oc_xxx \
  --time-range "近3天"
```

## 输出格式

拉取的消息会按以下格式组织:

```markdown
# 群名称 - 聊天记录

**时间范围**: 近3天
**时间段**: 2026-01-20 10:00 至 2026-01-23 10:00
**消息总数**: 156 条

---

## 2026-01-23

### 10:30 张三
@李四 你看一下这个 bug

---

### 10:32 李四
好的,我马上处理

---

...
```

## 工作原理

1. **搜索群聊** - 通过关键词搜索匹配的群聊
2. **选择目标** - 用户确认要拉取的群聊
3. **解析时间** - 支持自然语言描述("近3天")或精确时间戳
4. **拉取消息** - 自动分页,获取完整历史
5. **格式化输出** - 转换为易读的 Markdown 格式
6. **LLM 分析** - 输出可直接供 Claude 分析总结

## 支持的时间格式

- "近3天" / "最近3天" / "3天内"
- "近1周" / "最近1周"
- "本周"、"上周"
- "2026-01-01至2026-01-07"

## 技术架构

- **语言**: Python 3
- **依赖**: lark-cli (全局安装)
- **输出格式**: Markdown

## 目录结构

```
feishu-group-summary/
├── .claude-plugin/
│   └── plugin.json                    # 插件配置
└── skills/
    └── feishu-group-summary/
        ├── SKILL.md                    # 技能文档
        └── scripts/
            ├── lark_cli.py            # lark-cli 封装
            └── fetch_chat_messages.py # 核心脚本(搜索+拉取)
```

## 限制与注意事项

1. **权限要求**: 需要有权限访问目标群聊
2. **消息限制**: 最多拉取 10,000 条历史消息
3. **时间范围**: 部分群聊可能限制历史消息查看时长
4. **API 限流**: lark-cli 可能存在速率限制

## 作者

- 买峰 (maifeng@bytedance.com)
- DreamCats (https://github.com/DreamCats)

## 许可证

MIT License
